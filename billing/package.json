{
  "name": "catcut-billing-server",
  "version": "0.1.0",
  "private": true,
  "description": "Cat-Cut billing server (Stripe Checkout + license key issuing)",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "node --watch src/server.mjs",
    "start": "node src/server.mjs",
    "test": "node --test \"tests/*.test.mjs\"",
    "generate-keys": "node scripts/generate-keys.mjs"
  },
  "dependencies": {
    "stripe": "^22.3.1"
  }
}
