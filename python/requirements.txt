httpx>=0.27.0
budoux>=0.6.0
pyyaml>=6.0
kanjize>=1.0.0
silero-vad>=6.0.0
pillow>=10.0.0
# フェーズW24 Phase A: 顔検出(FaceDetectorYN/YuNet)。未導入環境でも顔検出スキップで動く(ベストエフォート)
opencv-python-headless>=4.10.0

# --- アーキテクチャ別の依存 ---
# Intel Mac (x86_64) は PyTorch の macOS 向け提供が 2.2.2 で終了しているため、
# torch/torchaudio/numpy を互換バージョンに固定する。
# torchcodec は Intel Mac 向けホイールが存在しない（torchaudio<2.9 なら不要）。
# faster-whisper は依存の onnxruntime に Intel Mac 向けホイールが無いため除外
# （ローカルWhisper STT は Apple Silicon のみ。既定の ElevenLabs STT は全環境で利用可）。

# Apple Silicon / その他のプラットフォーム
numpy>=2.0.0; platform_machine != "x86_64" or sys_platform != "darwin"
torchcodec>=0.10.0; platform_machine == "arm64" and sys_platform == "darwin"
faster-whisper>=1.1.0; platform_machine != "x86_64" or sys_platform != "darwin"

# Intel Mac (x86_64 / macOS)
numpy>=1.26,<2; platform_machine == "x86_64" and sys_platform == "darwin"
torch>=2.2,<2.3; platform_machine == "x86_64" and sys_platform == "darwin"
torchaudio>=2.2,<2.3; platform_machine == "x86_64" and sys_platform == "darwin"
